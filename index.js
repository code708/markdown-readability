// @ts-check
'use strict';

/**
 * @code708/markdown-readability — custom markdownlint rules
 *
 * This package exports custom rules that can be used with markdownlint.
 *
 * Usage:
 *   const customRules = require('@code708/markdown-readability');
 *   markdownlint.sync({ files: ['*.md'], customRules });
 */

// Export all custom rules as an array
module.exports = [];
